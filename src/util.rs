//! # `binsize::utils`
//!
//! Everything else that didn't fit into any of the other modules
//!

/// Represents soring order
#[derive(Copy, Clone)]
pub enum SortOrder {
    Ascending,
    Descending,
}
